# Phase 13 验收用例 — 手账海报「一图流」

> 自动化：`tests/test_poster.py`（7 例）+ staticmap 校验。120 passed 无回归。

## A. 自动化 ✅

| # | 覆盖 |
| --- | --- |
| A1 | 图片代理 URL 编码；staticmap 相对 URL 构造（pts/labels/days） |
| A2 | payload 按天分组、天内按 order 排序后重新编号、整体图+每天图 URL |
| A3 | staticmap 点位校验（格式/经纬度越界拒绝） |
| A4 | PosterStop/PosterData 默认值契约 |

## B. 端到端（线上，成都2日City Walk）

| # | 场景 | 结果 |
| --- | --- | --- |
| B1 | 攻略下点「🎨 生成手账海报」 | ✅ 后台抽取→高德定位→出海报消息 |
| B2 | 海报渲染 | ✅ 手账风：暖纸背景/胶带/楷体/Day 徽章/整体+每天路线地图/编号点位卡（实景图+类型标签+note+虚线连线）；标题「成都2日麻辣慢游手账」 |
| B3 | 地图标点 | ✅ 景点/餐馆/打卡都带编号标记+连线（高德静态图，后端签名，key 不进前端） |
| B4 | 图加载失败 | ✅ 回退 emoji 图标（onError，不再显示裂图） |
| B5 | 保存手账图片 | html2canvas 同源导出单张长 PNG，待用户实测 |

## 备注

- 结构化抽取依赖 LLM，个别地点高德查不到坐标会被舍弃（保证地图可标点）；
  某天点位可能偏少，属数据/模型波动。
- 高德静态地图 `/api/staticmap` 后端签名代拉；实景图走 `/api/img` 白名单代理。
