# Phase 7 验收用例 — 登录来源确认弹窗

> 自动化测试：`backend/tests/test_confirm.py`（4 例，sqlite 离线）。

## A. 自动化 ✅

| # | 场景 | 预期 |
| --- | --- | --- |
| A1 | 查确认回复 | 按 confirm_id 精确匹配；不同会话/不同 id 不串 |
| A2 | 用户点击后 wait_confirm | 返回所选 choice |
| A3 | 超时未点击 | 返回默认 skip |
| A4 | 最后一条是 action 消息 | running 仍为 true（后台在继续处理） |

## B. 端到端（线上已验证）

| # | 步骤 | 结果 |
| --- | --- | --- |
| B1 | 抓到知乎等需登录来源 | ✅ 出现确认卡片（问题 + 60s 提示 + 登录读取/跳过按钮） |
| B2 | 点「跳过」 | ✅ action 落库 → 「已跳过需登录的来源」→ 流程立即继续，最终 4 来源出攻略 |
| B3 | 同域名再次命中 | ✅ 不再重复询问（静默跳过） |
| B4 | 点「登录读取」 | 扫码卡出现 → 登录后重读该来源（需真实扫码，待用户验证） |
| B5 | 超时不点击 | 60s 后自动跳过（自动化 A3 覆盖，线上未专测） |

## 关联修复

- `ChromeMCP.call` 120s 兜底超时：修复 mcp 子进程僵死导致任务永久挂起
  （见 `docs/pitfalls/MCP调用无超时导致任务永久挂起.md`）。
- 服务器浏览器架构改为常驻 `travel-chrome.service`（9222）+ 只连不启，
  修复 profile 锁冲突（见 `docs/pitfalls/持久profile多会话启动锁冲突.md`）。

## 结果记录

- 2026-07-06：85 tests passed 无回归；B1-B3 线上验证通过（开封攻略用例）。
