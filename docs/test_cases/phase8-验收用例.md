# Phase 8 验收用例 — 携程城市动态解析

> 自动化：`tests/test_browser_tool.py::TestCtripCityResolve`（JS 构造/注入安全）、
> `tests/test_site_router.py::test_ctrip_city_cache`（三级缓存）。

| # | 场景 | 结果 |
| --- | --- | --- |
| A1 | 静态表城市（成都等 14 城） | ✅ 直接命中，不走解析 |
| A2 | 表外城市首次查询（开封） | ✅ 线上验证：「正在携程定位『开封』」→ 建议接口返回 331 → 抓到 8 家带实价酒店（¥277-456）→ DB 缓存 `开封=331` |
| A3 | 表外城市二次查询 | DB 缓存直接命中，不再解析（待日常使用验证） |
| A4 | 解析失败（生僻地名/接口变更） | progress 明确提示后回退公开搜索，不静默不阻塞 |
| A5 | 城市名含引号等注入字符 | ✅ json.dumps 转义（单测覆盖） |

## 结果记录

- 2026-07-06：88 tests passed 无回归；A2 线上端到端通过（含知乎登录确认卡、
  跳过后继续、最终生成攻略的完整链路）。
