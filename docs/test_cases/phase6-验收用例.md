# Phase 6 验收用例 — 携程酒店卡片定向抽取

> 自动化测试：`backend/tests/test_site_router.py` 新增 4 例（格式化、广告过滤、
> 不足 3 家判失败、抽取成功优先/失败回退），全部离线。

## A. 自动化 ✅

| # | 场景 | 预期 |
| --- | --- | --- |
| A1 | 卡片格式化 | 「携程实时酒店列表」清单；广告卡过滤；有价展示 ¥、无价标注「登录携程后可见」 |
| A2 | 过滤后不足 3 家 / 空列表 | 返回空串（视为抽取失败） |
| A3 | 抽取成功 | 来源 summary 用结构化清单，不走 LLM 摘要 |
| A4 | 抽取失败（DOM 改版/风控） | 回退整页快照 + LLM 摘要路径，不报错 |

## A2. 主动登录引导（自动化 ✅）

| # | 场景 | 预期 |
| --- | --- | --- |
| A5 | 抓到酒店但全部无价 | 打开携程登录页 → handoff 卡片（引导扫码拿实价）→ 登录后重抓，summary 带 ¥ |
| A6 | 不扫码等超时 | 按无价清单继续（不丢酒店数据、不报错） |
| A7 | 已有价格（已登录） | 不打开登录页、不发卡 |
| A8 | Action Guard 白名单 | click「扫码登录/二维码登录」精确放行；「扫码登录并绑定」/ fill 不放行 |

## B. 端到端手工验收

| # | 步骤 | 预期 |
| --- | --- | --- |
| B1 | 线上查「成都市中心的酒店」 | progress「已抓取携程实时酒店列表（N 家）」；来源 summary 含 ≥3 家真实酒店（名称/评分/点评数/位置）；回复引用真实酒店 |
| B2 | 扫码登录携程后再查 | 卡片带实价（¥xxx），回复引用真实价格 |

## 结果记录

- 2026-07-06：A 组 4 例 + A2 组 5 例通过（合计 76 passed 无回归）。
- 2026-07-06 线上验证（西安钟楼酒店查询）：无价触发登录引导，自动切到
  「扫码登录」tab，handoff 截图为携程扫码面板（大二维码可扫）；
  超时后按无价清单继续（杭州西湖用例验证）。
- 2026-07-06 B1 线上通过：抓到 8 家成都春熙路/太古里商圈真实酒店
  （美居/瑞城名人/全季/voco/明宇豪雅/兰博基尼/岷山饭店…，含评分与点评数），
  回复直接按这些酒店展开推荐。未登录无价格，已如实标注。
- 2026-07-06 B2 ✅：用户扫码登录后（持久 profile），香港酒店查询返回带实价的
  8 家真实酒店（¥747-¥1,083），不再出现登录卡。
- 2026-07-06 复合需求修复（用户反馈）：「规划一下+酒店预算」类请求此前只判成
  route 不触发携程。新增 `Preference.wants_hotel` + `resolve_wants_hotel` 规则兜底，
  复合需求两路都走；`decide_revision` 相应要求两类来源都覆盖才复用；
  ITINERARY_SYSTEM 增加「住宿推荐」小节（有携程实时列表时按预算选真实酒店）。
  线上用原话验证：携程触发、8 家带价酒店、攻略含住宿推荐小节（78 tests passed）。
