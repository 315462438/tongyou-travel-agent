# 长截图视觉读取修复（Phase 111）— 验收用例

对应计划：`docs/task_plans/长截图视觉读取修复-2026-08-27.md`
自动化：`backend/tests/test_image_prep.py`(13) + `test_vision.py` 新增 7 条 +
`test_image_failure_visible.py`(3)，全离线

## 一、切片规划（纯函数 `plan_tiles`）

| # | 用例 | 期望 | 自动化 |
| --- | --- | --- | --- |
| 1 | 高度未超上限 | 单片，不切 | `test_short_image_is_a_single_tile` |
| 2 | 20000px 高 | **末片结束位置 == 原图高度** | `test_tiles_cover_the_whole_height` |
| 3 | 相邻片 | 有重叠 | `test_tiles_overlap_so_a_line_at_the_seam_survives` |
| 4 | 片序 | 自上而下 | `test_tiles_are_top_to_bottom` |
| 5 | 任意高度 | 没有一片超上限 | `test_no_tile_exceeds_the_limit` |

> 用例 2 是全表最重要的一条。切片的失效方式不是报错，是**悄悄少读一段**。

## 二、规格化（`prepare_for_vision`）

| # | 用例 | 期望 | 自动化 |
| --- | --- | --- | --- |
| 6 | 规格内的图 | **字节完全不变**，不重编码 | `test_in_spec_image_is_returned_byte_identical` |
| 7 | 超高的图 | 切成多片，每片合规 | `test_tall_screenshot_is_split` |
| 8 | 片数超上限 | 每片仍合规 | `test_every_tile_is_within_the_limit_even_when_capped` |
| 9 | **片数超上限时先缩后切** | 最后一片是原图**底部** | `test_capped_tiling_shrinks_instead_of_dropping_the_tail` |
| 10 | 超宽的图 | 等比缩，不竖切 | `test_wide_image_is_scaled_not_sliced` |
| 11 | 解码失败 | 原样送（退化=改造前） | `test_undecodable_bytes_fall_back_to_passthrough` |
| 12 | 没装 Pillow | 原样送，不抛 | `test_missing_pillow_falls_back_to_passthrough` |
| 13 | 默认单边上限 | **严格小于 8192**（实测边界） | `test_max_side_default_leaves_headroom_under_the_measured_limit` |

> 用例 9 做过变异检验：把「先缩后切」改成「只切前 N 片」→ 该条立刻红，其余 12 条全绿。
> 也就是说这条护栏是真的，不是摆设。

## 三、调用侧接线（`vision.py`）

| # | 用例 | 期望 | 自动化 |
| --- | --- | --- | --- |
| 14 | 长截图 | 多片走**同一次**调用（`calls == 1`） | `test_tall_user_image_is_sent_as_multiple_tiles_in_one_call` |
| 15 | 切片时 | prompt 说明「同一张长截图」+ 片数 | `test_tiled_prompt_says_the_images_are_one_document` |
| 16 | 普通图 | prompt 里**没有**那段说明 | `test_normal_image_keeps_the_plain_prompt` |
| 17 | 页面类型判定 | 只送第一片（首屏够判） | `test_page_judge_only_needs_the_first_tile` |
| 18 | 长截图 | 播一条「分 N 段读」的进度 | `test_long_screenshot_announces_itself` |
| 19 | 普通图 | **不播**（别凭空制造等待预期） | 同上 |
| 20 | 8 天行程的逐日条目 | 一条不少 | `test_render_cap_does_not_truncate_an_eight_day_itinerary` |
| 21 | 渲染上限 | 可配置且 ≥40 | `test_render_cap_is_configurable_and_generous` |

> 用例 20 修的是另一个**我们自己加的**静默截断：`uniq[:15]` 把 8 天砍到 Day 2。
> 上限是渲染侧的，不是模型的产出上限——用户问「路线合不合适」，下游只看得到前两天。

## 四、失败必须可见

| # | 用例 | 期望 | 自动化 |
| --- | --- | --- | --- |
| 22 | `desc` 为空 | 走可见提示（`if desc:` 必须有 else） | `test_empty_description_tells_the_user` |
| 23 | 视觉抛异常 | 包住视觉调用的 try，**每个** handler 都通知用户 | `test_vision_exception_also_tells_the_user` |
| 24 | 提示消息 | 带 `meta`，否则被 `clear_plain_progress` 清掉 | `test_notice_carries_meta_so_it_survives_cleanup` |

> 这三条用 AST 断言**调用表达式**，不做文本匹配——本仓库反复踩过「断言匹配到了
> 自己写的注释」（CLAUDE.md 里记了四次）。

## 五、线上验证（人工，服务器）

用**线上真的挂过的那张图**（`1e15946939014d068733825b686c7c54`，1800×25242）走真实代码路径：

```bash
ssh "$DEPLOY_HOST" 'cd /home/ubuntu/travel-agent/backend && .venv/bin/python /tmp/verify111.py'
```

2026-08-27 实测：

| 项 | 修复前 | 修复后 |
| --- | --- | --- |
| 视觉调用 | 400 unsupported image | 4 片 / 一次调用 / 16–43s |
| 目的地 | 读不出（→ 反问用户） | 吉隆坡、仙本那、亚庇 |
| 日期 | — | 2026-10-01 … 10-08（8 天齐全） |
| 逐日安排 | — | Day1–Day8 全部（渲染上限修好之前只到 Day2） |

## 六、协议边界（人工，一次性，结论已固化进代码注释）

```
1800× 8192   941KB  OK
1800× 8500   988KB  FAIL
1024×14360  1207KB  FAIL   ← 压宽度没用，卡的是高度
1800× 6000   689KB  OK
```

判据只认 API 返回。**不要信 400 报文那句「格式不支持」**——格式是对的。

## 七、回退

`VISION_ENABLED=false` → 与改造前一致（带图消息按纯文本走）。
`VISION_MAX_TILES=1` → 不切片，长截图回到 400（仅用于复现）。
