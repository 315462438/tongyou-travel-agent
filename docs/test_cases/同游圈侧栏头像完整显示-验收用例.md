# Phase 85 · 同游圈侧栏头像完整显示验收用例

日期：2026-08-11

## 自动化用例

| 编号 | 场景 | 预期 | 自动化位置 |
| --- | --- | --- | --- |
| A-01 | 渲染左下角当前用户卡片 | 使用 `lg` 头像规格，不再复用 36px 默认规格 | `frontend/tests/visual-regressions.test.mjs` |
| A-02 | 侧栏头像尺寸 | 专属规则固定 46×46px 并带描边 | 同上 |
| A-03 | 非正方形或细节型头像 | 图片使用 contain + center，完整且不拉伸 | 同上 |
| A-04 | 其他头像场景 | 专属选择器不影响主页、好友和接力头像 | CSS 作用域检查 |

## 运行结果

```bash
cd frontend && npm test
# 58 passed

cd frontend && npm run lint
# 通过；仅保留项目已有的 2 条 Fast Refresh warning

cd frontend && npm run build
# 通过
```

## 人工烟测

1. 上传一张人物靠近画面边缘的头像；
2. 打开同游圈个人主页，确认主页大头像正常；
3. 查看左下角用户卡片，确认完整圆形和人物主体均可见；
4. 点击用户卡片，确认仍可进入“我的主页”；
5. 缩窄到手机布局，确认该卡片按原规则隐藏、底部导航不受影响。
