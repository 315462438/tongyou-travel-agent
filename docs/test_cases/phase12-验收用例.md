# Phase 12 验收用例 — 攻略配图

> 自动化：`tests/test_images.py`（12 例：占位符精确/模糊/无匹配/流式残片、
> 图片上下文组装、代理白名单含 SSRF 防护、终稿兜底插入）。114 passed 无回归。

## A. 自动化 ✅

| # | 覆盖 |
| --- | --- |
| A1 | `[[img:名称]]` 精确/包含匹配替换、无匹配删占位符 |
| A2 | 流式剥离行尾未闭合残片、不做兜底 |
| A3 | 图片上下文：多来源聚合、按名去重、URL 编码进代理、prompt 名单分景点/酒店 |
| A4 | 代理白名单：放行高德/携程域，拒绝伪造后缀与内网 IP（SSRF） |
| A5 | 终稿兜底：模型没插占位符时按 ### 标题/加粗/列表行插图、不重复、不乱插普通句 |

## B. 端到端（线上）

| # | 场景 | 结果 |
| --- | --- | --- |
| B1 | 黄山两天路线 | ✅ 模型主动插 4 张景点图（高德 POI），浏览器实测 4 张全加载、渲染 420px 圆角 cover |
| B2 | 苏州酒店查询 | ✅ 兜底为 3 家推荐酒店各插一张携程图，无残留占位符 |
| B3 | 图片代理 | ✅ 白名单内 200/jpeg，白名单外 400 |
| B4 | 导出长图 | html2canvas useCORS + 同源代理（图片不再污染 canvas），待用户实测 |

## 图源

- 景点图：高德 POI photos（首图，前 6 个景点）
- 酒店图：携程卡片 `<img>`（首图，前 6 家非广告）
- 均经 `/api/img` 同源代理，白名单 autonavi/amap/c-ctrip/tripcdn。
