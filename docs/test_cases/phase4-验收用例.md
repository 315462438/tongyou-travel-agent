# Phase 4 验收用例 — 记忆系统（+ 修复：多轮修改判定 / 表格渲染）

> 自动化测试：`backend/tests/test_memory.py`（9 例）+
> `backend/tests/test_orchestrator_revision.py`（5 例），全部离线（sqlite 内存库 + fake LLM）。
> 运行：`cd backend && .venv/bin/python -m pytest tests/ -q`

## A. 记忆写入（自动化 ✅）

| # | 场景 | 预期 |
| --- | --- | --- |
| A1 | add 操作 | 新记忆入库，带 source_conversation_id |
| A2 | update / delete（带 id） | 内容更新 / 记录删除 |
| A3 | 脏输入（空内容 add、不存在的 id、非法 op、非法 type） | 全部安全跳过，非法 type 归一化为 preference |
| A4 | 提炼 prompt | 已有记忆的 id 必须进 prompt（模型才能给出 update/delete） |

## B. 记忆读取 / 注入（自动化 ✅）

| # | 场景 | 预期 |
| --- | --- | --- |
| B1 | 空记忆 | 注入块为空串，不污染 prompt |
| B2 | 有记忆 | 生成「长期记忆」注入块，带类型标签 |
| B3 | 历史会话检索 | 优先标题含目的地的会话；无匹配按最近兜底；排除当前会话；无助手回复的会话跳过 |

## C. 多轮修改判定修复（自动化 ✅，对应用户反馈 bug）

| # | 场景 | 预期 |
| --- | --- | --- |
| C1 | 只有携程酒店来源 + 「规划一下行程」 | **不复用**，重新走小红书路由 + 搜索 |
| C2 | 只有攻略来源 + 「帮我查酒店」 | 不复用，走携程路由 |
| C3 | 攻略来源 + 攻略修改请求 | 复用，跳过搜索 |
| C4 | 换目的地 / 新旧目的地未知 | 一律不复用 |

## D. 端到端手工验收

| # | 步骤 | 预期 |
| --- | --- | --- |
| D1 | 会话中说「我特别爱吃辣，不太能吃甜」 | 回复末尾出现「🧠 已记住：…」；GET /api/memory 出现对应记忆 |
| D2 | 新会话说「按我的口味推荐」 | 助手消息上方「🧠 记忆 · N」可展开，规划结果体现爱辣忌甜 |
| D3 | 说「我现在能吃甜了」 | 记忆被 update 而非重复 add |
| D4 | 侧边栏「🧠 记忆」 | 面板列出全部记忆，可删除 |
| D5 | 含预算表的攻略 | Markdown 表格正常渲染为带边框表格（remark-gfm） |

## 结果记录

- 2026-07-06：A/B/C 全部 14 例自动化通过；存量 42 例不回归（合计 56 passed）。
- 2026-07-06：D5 已验证（香港会话预算表渲染正常）。
- 2026-07-06：D1 冒烟通过：香港会话中说「我特别爱吃辣，不太能吃甜…」→
  回复按口味重排（辣味餐饮进预算、避坑提示含辣度要求）→ 消息末尾
  「🧠 已记住：用户特别爱吃辣；用户不太能吃甜」→ GET /api/memory 两条
  preference 记忆入库（浏览器截图 + API 验证）。
- D2-D4 待日常使用验证。
