# Task Plan · 体验走查修复批次（2026-07-30）

依据：`docs/dev_docs/体验走查-2026-07-30-真实用户视角.md`。用户指示「全部修改」。

## 目标与方案

| # | 问题 | 方案 | 涉及模块 |
| --- | --- | --- | --- |
| P0-1 | 深度推理终稿取到收尾寒暄，长报告被丢 | `_extract_answer` 改为在末尾几条 AI 消息里择优（明显更长/更结构化的优先）；RESEARCH_SYSTEM 禁止报告后追加单独收尾消息 | `app/agent/deep_research.py` |
| P0-2 | 兜底插图/占位符把表格劈开 | `_embed_images` 全面表格安全化：表格行内的占位符与兜底命中一律推迟到表格结束后插入 | `app/agent/orchestrator.py` |
| P0-3 | `¥400~600` 被 GFM 单波浪线吃成删除线 | remark-gfm 关闭 singleTilde（`~~del~~` 保留） | `Home.tsx` `Trips.tsx` |
| P1-1 | 小红书凑 5 篇抓 11 篇（串行 20s/篇） | `collect_xhs_sources` 尝试上限 = n+2；逐篇进度回调 | `app/tools/xhs_mcp.py` `orchestrator._collect_xhs` |
| P1-2 | 「当前动作」卡 3 分钟不动 | 上面的逐篇进度（每 ~20s 一条实质进度） | 同上 |
| P1-3 | 「通常 2 分钟」「快好了」过度承诺 | `THINKING_EXPECTED_SEC.智能规划` 140→260、hint 改「通常 3-4 分钟」；空态卡片时长同步 | `interaction.ts` `Home.tsx` |
| P1-4 | 360 兜底把广告位当结果 | 结果标题与查询词元重叠校验（`_relevant_to_query`） | `app/tools/browser_tool.py` |
| P1-5 | agent 白试 Google 被 url_guard 拦 | RESEARCH_SYSTEM 明确禁止 open_page 搜索引擎 URL | `deep_research.py` |
| P2-a | 灵感配图与正文打架（自我打脸） | 兜底插入的小红书灵感图下加可见图注 `*图源：小红书笔记「标题」*`，明确它是笔记配图而非本日路线 | `orchestrator._embed_images` |
| P2-b | 正文预算 vs 面板预算差 1000+ | 抽取 prompt 要求逐项覆盖；schema 加 `guide_stated_total`，重算差 >20% 时面板自动加口径说明 | `budget.py` `budget_schema.py` |
| P2-c | 表格里 `**加粗**` 失效（CJK 标点相邻） | 渲染前 `fixMarkdownEmphasis`：`**…**` 内侧包零宽空格使定界符两侧成立 | `interaction.ts` + 两个渲染点 |
| P2-d | `[img:...]` 单括号占位符漏进正文 | `_IMG_PLACEHOLDER` 兼容单括号与全角冒号；流式残片剥离同步 | `orchestrator.py` |
| P2-e | 「因资料不含携程列表…」内部实现外露 | ITINERARY_SYSTEM 住宿条款补一句：不向用户解释资料缺失的内部原因 | `orchestrator.py` |
| P2-f | 快答说「春节」（问的国庆） | QUICK_TAKE_SYSTEM 补时间一致性约束 | `deep_research.py` |
| P3-a | 邀请码报错没出路 | 报错文案补「找管理员获取」 | `auth_api.py` |

明确**不改**的两项（走查报告 P3）：
- 终稿后正文仍增长但「停止」消失 —— 复核为 Phase 14 静默反思的设计行为（反思在终稿后就地改稿，不占流式位），保留。
- 海报 880px 固定宽在窄容器横向滚动 —— Phase 13 设计如此（`overflow-x:auto`），保留。

## 落地补充（实施中新增）

- **P1-4 扩展**：必应路径也命中过「搜索框正常但结果全是 Facebook 链接」的垃圾页
  （框校验只证明页面对应查询，不证明结果相关）→ `_relevant_to_query` 同样应用于必应
  结果；全滤空视为垃圾页，走重试/360 兜底。
- **P0-1 返工一次**：初版「全局取最长 AI 消息」把注入历史里的旧攻略当成终稿（上线即翻车），
  改为只在最后一条 human 之后的消息里择优。教训见
  `docs/pitfalls/深度研究终稿择优不能全局取最长.md`。

## 验收结果（2026-07-30 线上复测）

- 后端 567 例全绿（含本批次 17 例新增）；前端 32 例全绿。
- 攻略轮：4 张表全部完整渲染、无占位符泄漏、无裸星号、无误删除线、无内部实现话术；
  小红书阶段逐篇进度每 ~20s 推进；首字 4:10 → ~3:20。
- 深度推理轮：终稿为完整 12 章节对比报告（修复前被收尾寒暄/历史旧攻略顶掉）。
- 直答轮（开关误关时）：表格/加粗渲染正常，约 40s 完成。

## 验收标准

- 后端：`tests/test_ux_fixes.py` 全绿（表格安全插图、单括号占位符、图注、xhs 尝试上限、360 相关性过滤、深度研究终稿择优、预算口径注记）。
- 前端：`tests/markdown.test.mjs` 全绿（singleTilde 关闭已在组件层，测 fixMarkdownEmphasis 纯函数）。
- 现有全部单测不回归。
- 部署后线上复测一轮成都攻略：表格完整、无占位符泄漏、无删除线吞字。
