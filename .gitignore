# ---- 密钥与本地配置（绝不提交）----
.env
.env.*
!.env.example
*.pem
*.key

# 凭据交接类文档一律不进仓库（真实凭据请放在仓库之外）
*凭据*
*credential*
*secret*
*.secrets

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# ---- Node / 前端 ----
node_modules/
frontend/dist/
.vite/
*.tsbuildinfo

# ---- 构建产物 ----
# backend/static 是 frontend 构建产物的拷贝（部署时生成），不进版本库。
# 部署流程见 CLAUDE.md：npm run build && cp -r dist/. ../backend/static/
backend/static/

# ---- IDE / 系统 ----
.idea/
.vscode/
.DS_Store

# ---- 运行时产物 ----
*.log
/tmp/
backend/*.db
backend/*.sqlite3

# ---- 评估集样本 ----
# 抽取评估的输入是真实线上攻略正文（真实用户会话的产物）。已核对不含手机号/
# 证件号/邮箱，但没有必要把用户内容放进代码仓库。用 `python -m evals.fetch_samples`
# 按 extract.yaml 里登记的 message_id + sha256 拉取。
backend/evals/samples/*.md
!backend/evals/samples/README.md
