fastapi>=0.115
python-multipart>=0.0.9
uvicorn[standard]>=0.32
sqlalchemy>=2.0
psycopg[binary]>=3.2
pydantic>=2.9
pydantic-settings>=2.6
openai>=1.50
mcp>=1.2
python-dotenv>=1.0
httpx>=0.27
websockets>=13.0
pytest>=8.0
pytest-asyncio>=0.24
langgraph>=0.2
langgraph-checkpoint-postgres>=2.0
psycopg-pool>=3.2
deepagents>=0.6
langchain-deepseek>=1.0
# 2026-08-14 锁死：langchain-core 1.4.x 的回调管理器开始对 handler 无条件读
# ignore_chain/raise_error 等属性，鸭子类型 handler（SubagentTracker）缺属性即炸。
# 锁住当前实测工作版本，防 `>=` 漂移升级再踩（升级需显式改这里并重测）。
langchain-core==1.4.9
langfuse>=4.14
pyyaml>=6.0  # evals 评估集读 queries.yaml
# Phase 111：长截图必须切片后才能进视觉模型（单边上限 8192px），切片要解码，
# stdlib 没有图像编解码器。
Pillow>=10.4
