# 复制为 frontend/.env.local 后填入真实值。**.env.local 不进版本库**
# （frontend/.gitignore 的 `*.local`）。自己去高德开放平台申请，别用别人的。

# 高德「Web端(JS API)」key —— 行程板互动地图用（components/TripMap.tsx）。
# 控制台 https://console.amap.com/dev/key/app：应用 → 添加 Key → 服务平台选「Web端(JS API)」。
#
# ⚠️ 这个值**本质是公开的**：它必须下发到每个访客的浏览器，F12 就能看到，藏不住。
# 把它挪出源码是为了让 fork 的人默认用自己的 key，不是为了保密。真正的防护是在
# 控制台给这个 Key 配**域名白名单**（「设置」→ 安全设置），限制到你自己的域名。
#
# ⚠️ **构建时必须注入**：Vite 在 `npm run build` 时把它编译进产物。漏了不会报错，
# 只会让互动地图静默回退成静态图——所以 TripMap 会在控制台打一条 error。
VITE_AMAP_JS_KEY=

# 上面那个 key 配对的「安全密钥」（控制台同一行的第二列）。
#
# ⚠️ **这个才是真密钥，绝不能进仓库、绝不能进生产构建。**
# 只有本地开发（`npm run dev`）用它明文直连高德。生产走 nginx 的 `_AMapService`
# 反向代理，由服务端把 jscode 拼进请求（高德官方的「代理服务器」模式）——
# 前端那段代码在 `import.meta.env.DEV` 分支里，生产构建会摇树剔除。
#
# 服务器侧对应配置在 /etc/nginx/sites-enabled/default：
#   location /_AMapService/ { set $args "$args&jscode=<安全密钥>"; ... }
VITE_AMAP_JS_SECURITY_CODE=
